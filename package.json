{
  "name": "nativescript-aspect-ratio-calculator",
  "version": "1.0.0",
  "description": "Aspect Ratio Calculator",
  "main": "arc/arc.js",
  "scripts": {
    "start": "babel arc -d lib && node lib/arc.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel arc -d lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "6.23.0",
    "babel-preset-env": "1.2.1"
  }
}
